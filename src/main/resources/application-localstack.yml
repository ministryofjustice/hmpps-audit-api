graceful:
  shutdown.enabled: false

aws:
  provider: localstack

hmpps.sqs:
  provider: localstack
  queues:
    auditQueue:
      queueName: mainQueue
      asyncQueueClient: true
      dlqName: mainDlq

spring:
  jpa:
    show-sql: true
