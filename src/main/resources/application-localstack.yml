graceful:
  shutdown.enabled: false

aws:
  provider: localstack

hmpps.sqs:
  provider: localstack
  queues:
    auditqueue:
      queueName: mainQueue
      asyncQueueClient: true
      dlqName: mainDlq
    auditusersqueue:
      queueName: auditUserQueue
      asyncQueueClient: true
      dlqName: auditUserDlq

spring:
  jpa:
    show-sql: true
